[servers]
# Replace HOST_IP with your EC2 public IP after running terraform apply
# Or dynamically from terraform output in CI
app-server ansible_host=HOST_IP ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/id_rsa

[servers:vars]
ansible_python_interpreter=/usr/bin/python3
